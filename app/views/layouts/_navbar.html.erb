<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">RailsFitness</a>
      <% if logged_in? %>
        <%= link_to 'Logout', :logout, class: "btn btn-default navbar-btn", method: :post %>
        <p class="navbar-text navbar-right">Signed in as <%= current_user.username %></p>
      <% else %>
        <%= link_to 'Login', :login, class: "btn btn-default navbar-btn" %>
      <% end %>
    </div>
  </div>
</nav>